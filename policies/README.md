# IAM policies

This folder contains redacted IAM policies for the DevSecOps lab.
Account IDs are replaced with `<ACCOUNT_ID>`, bucket suffixes with `<SUFFIX>`.

- `lab-weak-user-policy.json` — the deliberately weak identity used in the controlled exercise.
